/* ============================================================
   設定檔 —— 只要改這個檔，其他都不用碰
   改完存檔，再用 FTP 重新上傳這個 config.js 就會生效。
   ★ 注意：每行結尾的逗號、文字兩邊的引號 " " 都要保留。
   ============================================================ */
const CONFIG = {

  // 1) 查詢網址（設定一次）：貼上你的 Apps Script「/exec」網址
  endpoint: "貼上你的_exec_網址",

  // 2) 網頁標題：想改成自己的名稱就改這裡
  titleZh: "小樽導生密碼查詢",
  titleEn: "Advising Password",

  // 3) 主題顏色（可不改）：留著就是清華紫；想換色就填色碼，例如 "#1f6e72"
  accentColor: "#82318e",

};
